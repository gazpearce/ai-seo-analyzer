image: gitpod/workspace-full
tasks:
  - name: Start static server
    command: |
      # Serve the project root on port 8000 and bind to all interfaces so Gitpod preview works
      python3 -m http.server 8000 --bind 0.0.0.0
ports:
  - port: 8000
    onOpen: open-preview